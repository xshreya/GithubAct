name : Python script

on:
  push:
    branches: 
      - main

jobs:
  runs-on: ubuntu-latest
  env :
    name: "Shreya"
    role: "Developer"

  strategy:
    matrix: 
      node-version: [12.x, 14.x, 16.x]

  steps: 
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: print
      run: |
        echo "hello"
      

